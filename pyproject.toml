[project]
name = "winmem-vol3-mcp"
version = "0.1.0"
description = "MCP server wrapping Volatility3 for conversational Windows memory forensics"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "volatility3==2.27.0",
]
